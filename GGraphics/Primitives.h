#pragma once

enum class PrimMesh
{
	Square,
	Circle,
	Plane
};


// Class should create the correct mesh given the above enums
// and return it as a mesh? So instead of loading a mesh
// we create one.

class Primitives
{
	Primitives();
	~Primitives();
};

